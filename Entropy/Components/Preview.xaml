<UserControl x:Class="Entropy.Components.Preview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:Entropy.ViewModels"
             xmlns:components="clr-namespace:Entropy.Components"
             mc:Ignorable="d"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             d:DataContext="{d:DesignInstance viewModel:EntropyCore}"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <components:RendererSelector x:Key="RendererSelector" />
    </UserControl.Resources>
    <Grid>
        <ContentControl Content="{Binding SelectedObject, Mode=OneWay}"
                        ContentTemplateSelector="{StaticResource RendererSelector}" />
    </Grid>
</UserControl>
